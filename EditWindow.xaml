<Window x:Class="WarehouseManagement.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chỉnh sửa thông tin nón" 
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#f5f6fa">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980b9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#3498db" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✏️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Chỉnh sửa thông tin nón" FontSize="18" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="25">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã nón:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtId" Grid.Row="0" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5" IsEnabled="False"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Loại nón:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtHelmetType" Grid.Row="1" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Kích thước:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtSize" Grid.Row="2" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Hãng:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtBrand" Grid.Row="3" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ngày nhập:" VerticalAlignment="Center" Margin="0,8"/>
                <DatePicker x:Name="dtpImportDate" Grid.Row="4" Grid.Column="1" Height="35" Margin="0,5" FontSize="14"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Chất liệu:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtMaterial" Grid.Row="5" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Giá (VND):" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtPrice" Grid.Row="6" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="7" Grid.Column="0" Text="Màu sắc:" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtColor" Grid.Row="7" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>

                <TextBlock Grid.Row="8" Grid.Column="0" Text="Cân nặng (kg):" VerticalAlignment="Center" Margin="0,8"/>
                <TextBox x:Name="txtWeight" Grid.Row="8" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,5"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="❌ Hủy" Style="{StaticResource ModernButton}" Background="#95a5a6" FontSize="14" Padding="20,10" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="💾 Lưu thay đổi" Style="{StaticResource ModernButton}" Background="#2ecc71" FontSize="14" Padding="20,10" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
